For identifying a dp problem, tere are two ways we have 2 choices either we can choose something or not, so we have potential to use recurssion, and in the recurssion there will be overlapping subproblems thats why we will use dp, and an optimal solution will be asked to us, largest, smallest, best this kind of

Variants: 
1. 0/1 Knapsack problem:
It has subproblems such as
subset sum problem
equal sum partition problem
count of subset sum with a given sum
minimum subset sum difference
count the number of subset with a given diff
target sum

2. unbounded knapsack

* Fractional knapsack (Greedy)